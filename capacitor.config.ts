import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.lunarwalrusroam',
  appName: 'lunar-walrus-roam',
  webDir: 'dist'
};

export default config;
