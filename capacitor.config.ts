import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.almoxarifadoyees',
  appName: 'Almoxarifado Yees!',
  webDir: 'dist'
};

export default config;
